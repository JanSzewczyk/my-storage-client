export declare type SelectOption = {
  key: string;
  value: string;
};

export declare type SelectConfig = {
  placeholder?: string;
  type: string;
  name: string;
  readOnly?: boolean;
  disabled?: boolean;
};
